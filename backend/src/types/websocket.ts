import { WebSocketMessage, AnalysisProgress, AnalysisResult, WebSocketAuthPayload } from 'shared-types';

// Re-export for backward compatibility
export type { WebSocketMessage, AnalysisProgress, AnalysisResult, WebSocketAuthPayload };
